const job = `agenda.job`

export const AgendaAttributes = {
  JOB_ID: `${job}.id`,
  JOB_NAME: `${job}.name`,
  JOB_OPTS: `${job}.opts`,
  JOB_TYPE: `${job}.type`,
  JOB_PRIORITY: `${job}.priority`,
  JOB_REPEAT_INTERVAL: `${job}.repeat_interval`,
  JOB_REPEAT_TIMEZONE: `${job}.repeat_timezone`,
  JOB_REPEAT_START_DATE: `${job}.start_date`,
  JOB_REPEAT_END_DATE: `${job}.end_date`,
  JOB_NEXT_RUN_AT: `${job}.next_run_at`,
}
